# Warm Haven — 프로젝트 지침

> 전역 `~/.claude/CLAUDE.md`(환각 방지·증거 정책·보안·파괴적 작업 확인)를 그대로 따른다.
> 아래는 **이 프로젝트에만 추가되는 사실과 제약**이다. 전역 내용은 여기서 다시 쓰지 않는다.

## 프로젝트 한 줄 정의

성인 전용 **안전 매칭 앱**(Warm Haven)의 백엔드. FastAPI + SQLite(로컬), `/v1` 라우터 + 정적 프론트.
GPS 없이 선택 지역·프로필 조건으로 사람을 찾고, **상호 좋아요 이후에만** 채팅을 허용하는 MVP.

## 협업 방식 — 제안은 자유롭게, 행동은 보수적으로

이 한 축이 "Claude의 추론을 막지 않되 보안·안전을 우선한다"를 동시에 만족시킨다.

- **사고·제안·대안 제시에는 자율성을 가진다.** 사용자의 말을 그대로 따르기만 하지 않는다.
  실질적으로 더 나은 방법이 보이면 **"이렇게 하는 게 더 좋다 — 왜(근거), 어떻게(방법), 어떤 리스크를 피하는지"**를 먼저 제시한다.
- 단, 이는 **판단 근거·트레이드오프·버린 대안**을 짧게 공유하라는 뜻이지, 사고과정 전체를 덤프하라는 뜻이 아니다(전역 §6 유지).
- 캘리브레이션: 사소한 것마다 반론하지 않는다. 실질적 차이가 나는 지점에서만 제안하고, **사용자가 결정하면 그 결정을 따른다.**
- **파괴적·외부 영향 작업(삭제·덮어쓰기·일괄 변경·외부 전송)의 실행에는** 자율성을 적용하지 않는다. 전역 규칙대로 확인 절차를 우선한다.

## 아키텍처 불변식 (코드 작업 시 반드시 일관 유지)

- **권한 판정은 요청 본문의 ID가 아니라 인증된 프로필 기준으로 백엔드에서 결정한다.** (`app/core/security.py`의 `get_current_profile`; actor 필드를 요청 본문에 두지 않는다 — people/likes/chats/safety/private-photos 일관.)
- **차단 관계는 검색·매칭·채팅 조회 전부에 적용한다.** 공통 판정은 `app/services/social.py`.
- **데모/미연결 기능은 명확히 "데모"로 표시한다.** 운영 인증·암호화가 연결되지 않은 것을 작동하는 것처럼 제시하지 않는다.
- 슬라이스 단위로 데모 → 인증·DB 저장으로 전환해 왔다(1~3: 인증/people/likes/chats, 4~6: safety/private-photos). 진행·보류 상태는 `docs/FINAL_HANDOFF.md`·`WORKSPACE.md`가 기준.

## 안전 도메인 제약 (제품 성격상 절대)

- 성매매·스토킹·불법 촬영물 유통을 **조장하는 기능을 추가하지 않는다.**
- 정확한 위치 노출, 백그라운드 위치 추적, 공개 연락처 게시를 **기본값으로 추가하지 않는다.** (GPS·카메라 권한은 현재 의도적으로 차단.)
- 비밀 사진은 `공개 요청 사진`으로 표현하고, **상대별 승인·철회** 구조를 유지한다.
- 신고 증거는 일반 채팅과 분리하고 접근 권한을 제한한다.

## 검증

```powershell
python -m unittest discover -s tests -t .
```

`-t .`(top-level=프로젝트 루트)가 **반드시** 있어야 `tests/__init__.py`가 실행되어 임시 SQLite로 격리된다.
생략하면 운영 `warm_haven.db`가 오염되고 테스트가 플래키해진다.

## git / 비밀값

- 원격: **비공개 저장소 `Apelmedia/Ttalk`(GitHub)에 push 허용.** 이력 보존은 git 커밋으로 충분하므로 별도 `.bak` 백업 의식은 생략한다.
- **비공개 ≠ 비밀**이다. 한 번 push된 히스토리의 시크릿은 지워도 유출로 간주한다. 따라서 아래 커밋 금지 항목을 절대 준수한다.
- **절대 커밋·push 금지:** 비밀값·API 키·Firebase 서비스 계정·운영 DB URL, 그리고 **실 사용자 데이터**(`warm_haven.db` 등 DB 파일·실데이터 픽스처). 이 앱은 성적 지향 등 특별 카테고리 개인정보를 다루므로 민감 데이터는 코드가 아니라 DB에만 둔다. (`.env`·`*.db`는 `.gitignore` 대상, `.env.example`만 추적.)
- 커밋·브랜치·push 등 git 작업은 사용자가 요청할 때 수행한다.
